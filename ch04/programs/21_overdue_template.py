# This program outputs the titles of all overdue books.

# Variable declarations
# today    : int    (Today's date)
# titles   : list   (List of loaned book titles)
# duedates : list   (List of corresponding due dates)
# index    : int    (Index for iterating through list)

# Input
today = int(input("Enter today's date in YYYYMMDD: "))
titles = open("titles.txt").read().splitlines()
duedates = open("duedates.txt").read().splitlines()

# Process and Output
[[...Fill in this section...]]
